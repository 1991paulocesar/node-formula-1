info:
  name: delete-drivers-id-3
  type: http

http:
  method: DELETE
  url: "{{base_url}}/drivers/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
